default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password:
  host: db
  timeout: 5000

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
